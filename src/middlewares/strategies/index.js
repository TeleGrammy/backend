const googleStrategy = require("./googleStrategy");
const githubStrategy = require("./githubStrategy");
const faceBookStrategy = require("./facebookStrategy");

module.exports = {googleStrategy, githubStrategy, faceBookStrategy};
